import { Font20Directive } from './font-20.directive';

describe('Font20Directive', () => {
  it('should create an instance', () => {
    const directive = new Font20Directive();
    expect(directive).toBeTruthy();
  });
});
